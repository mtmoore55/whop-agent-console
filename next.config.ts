import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev badge sits on top of the sidebar's bottom-left controls.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
